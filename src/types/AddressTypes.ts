export type Input = {
    address: string;
    netmask: number;
};

export type BinaryData = {
    netmaskBin: string;
    addressBin: string;
};

export type NetworkParameters = {
    networkAddress: string;
    broadcastAddress: string;
};
